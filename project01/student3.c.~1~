#include <stdio.h>
#include <assert.h>
#include "functions.h"

/* Student tests for function 3 */

int main(void) {
  assert(pell(1) == 1); 
  assert(pell(2) == 2);
  assert(pell(3) == 5); 
  assert(pell(5) == 29);
  assert(pell(6) == 70); 
  assert(pell(7) == 169); 
  
  printf("\nThis test has passed!\n");
}
